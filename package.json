{
  "private": true,
  "devDependencies": {
    "lerna": "3.19.0"
  },
  "scripts": {
    "release": "lerna publish from-package --skip-git",
    "next": "lerna publish from-package --dist-tag next --skip-git"
  }
}
